name: Weekly Algorithmic Insurance Check

on:
  schedule:
    - cron: '0 8 * * 1' # Weekly on Monday at 8:00 AM UTC
  workflow_dispatch:

jobs:
  check-insurance:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 9.1.0

      - name: Install dependencies
        run: pnpm install

      - name: Evaluate traffic drop triggers
        env:
          GOOGLE_SERVICE_ACCOUNT_EMAIL: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_EMAIL }}
          GOOGLE_PRIVATE_KEY: ${{ secrets.GOOGLE_PRIVATE_KEY }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          echo "Scanning GSC traffic drops limits..."
          # Mock script execution checks
          # In production, queries GSC searchanalytics API, compares rolling clicks drop,
          # and if drop >15%, calls DB createClaim() mutation and assigns a recovery pod.
